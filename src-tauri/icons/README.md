# App Icons

The icon set was generated from the project-owned `app-icon.png` source image with:

```sh
npm run tauri -- icon app-icon.png
```

The design is an original mesh-cube/export mark for MidasGtsExporter.
